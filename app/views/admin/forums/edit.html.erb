<h1>Admin::Forums#edit</h1>

<% form_for :forum,
     :url  => admin_forum_path(@forum),
     :html => { :method => :put } do |f| -%>
<%= render :partial => "form", :object => f %>
<%= submit_tag 'Save'[:save_forum], :or => link_to('Cancel'[:cancel], admin_forums_path) %>
<% end -%>
