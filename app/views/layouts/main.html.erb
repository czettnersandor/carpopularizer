<% if logged_in? %>
  Welcome <%= current_user.login %>
  <%= link_to("Logout", "logout") %>

<% else %>
  <%= link_to("Login", "login") %>
<% end %>

<% if flash[:login_message] %>
  <%=flash[:login_message]%>
<%end%>

<%= @proba %>
Layout
<%= yield %>
